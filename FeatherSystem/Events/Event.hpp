#pragma once

// Events module.
//
// FSEvent/FSEvents are layered on top of the scheduler and remain optional for
// consumers that only need Core or Scheduler.
#include "../FeatherRuntime/FSEvent.hpp"
