#pragma once

// Scheduler module.
//
// Keeps the v1 FSScheduler API available while giving consumers a stable
// module include path that depends only on Core/Clock.
#include "../FeatherRuntime/FSScheduler.hpp"
